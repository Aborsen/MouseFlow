export const DEFAULT_START_MONTH_DATE = new Date(
  new Date().getFullYear() - 1,
  0
);

export const DEFAULT_END_MONTH_DATE = new Date(new Date().getFullYear() + 1, 0);

export const DEFAULT_DATE_FORMAT = 'dd/MM/yyyy';
