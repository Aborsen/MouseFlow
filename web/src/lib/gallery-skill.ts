/* Что уезжает в галерею, в форме, которую галерея принимает.
 *
 * Реализация лежит в `api/_gallery-skill.mjs` - по той же причине, что flow-for.ts и skill-schema.ts: правило
 * читают обе стороны (маршрут проверяет формат, приложение его ставит), а serverless-функция не может
 * импортировать из исходников приложения с уверенностью в том, что протянет бандлер. Один ответ, один файл.
 */
export { SKILL_FORMAT, skillForGallery } from '../../../api/_gallery-skill.mjs';
